ROLLE & ZIEL
Redraw-Bug FIXEN — die Ursache ist strukturell verifiziert (Windows-Messung, report 2026-07-09):
`wx/qt/canvas.rkt` öffnet nie eine retained-Backing-Sitzung, dadurch verwirft jeder Flush-Zyklus die
Backing-Bitmap; nach einer Teil-Invalidierung (Caret-Blink, eine getippte Zeile) bleibt der Rest
weiß. Fix = das Muster der nativen Backends spiegeln. Danach auf allen drei Plattformen validieren,
Reihenfolge Windows (Fix) → Linux (Validierung) → macOS (Validierung + macOS-only-Beobachtungen).

MODUSWECHSEL: Diese Session FIXT (mit Vorher/Nachher-Messung), sie misst nicht nur. Der Fix ist rein
im Backing-/Blit-Pfad und berührt das Event-Loop-Modell NICHT.

STAND (git-verifiziert)
- origin/qt-backend enthält die vier Menü-/Startup-Fixes + die gated PLT_QT_DEBUG-Diagnose-Hooks aus
  der 07-09-Session (canvas.rkt: refresh/flush/begin-/end-refresh-sequence/queue-backing-flush;
  shim.cpp: paintEvent/blit_argb). Diese Hooks bleiben und liefern den Vorher/Nachher-Beweis.
- Clean-Start ist auf allen drei Plattformen sauber (keine offene Landmine).
- Konsum: Windows = Installation-wide-Link (→ raco setup); macOS/Linux = -S-Source-Override (→ raco make).

VORBEDINGUNG (Nutzer erledigt separat): DrRacket ist auf allen drei Maschinen explizit auf LIGHT MODE
gesetzt (Preferences, NICHT „Follow OS") — kontrollierte helle Baseline, damit „weiße Lücke = leere
Bitmap" eindeutig bleibt und nicht mit Dark-Mode-Hintergrund verwechselt wird. Phase 0 verifiziert das.

HARTE GUARDRAILS
- Kein exec()/QEventLoop/geschachtelte Schleife. cocoa/gtk/win32 NICHT anfassen (nur als Referenz
  lesen). Zwei-Repo-Commits (Submodul zuerst). Nur ff-Pulls/-Pushes. Gated Diagnose bleibt gated.
  Report-Header = gemessene racket --version.
- macOS-Beobachtungen (Befund A / Statuszeile, unten): BEOBACHTEN, nicht fixen. Sieht ein Befund nach
  echtem Bug aus → STOPP und berichten, KEIN eigenmächtiger Fix (das ist eine spätere eigene Session).
- Erfolgskriterium der Session = Redraw-Fix auf allen drei Plattformen. Die macOS-Nebenbefunde
  blockieren das Schließen des Redraw-Themas NICHT, falls sie unklar bleiben.

── WINDOWS (Fix) ────────────────────────────────────────

PHASE 0 — verifizieren
- racket --version = v9.2 [cs] (gemessen). Git beide Repos sauber, auf aktuellem qt-backend.
- Shim aktuell bauen falls nötig; Bytecode aktuell.
- DrRacket-Theme: bestätigen, dass Light Mode aktiv und explizit gesetzt ist (nicht OS-gesteuert).

PHASE 1 — VORHER-Messung (Baseline festhalten)
- Repro aus dem Report: mehrere `(define … N)`-Zeilen via echte Keystrokes (SendKeys, Klammern
  escaped `{(}`/`{)}`, vorher explizit in den Definitions-Editor klicken — Nebenbefund §5 des Reports).
- Mit PLT_QT_DEBUG die vier Diskriminatoren + Screenshot festhalten: nur letzte Zeile sichtbar, Rest
  weiß; retained-Zustand (retained-counter 0). Das ist der Vorher-Zustand.

PHASE 2 — FIX (gespiegelt an win32)
- `wx/qt/canvas.rkt`: `start-backing-retained` einmalig nach `qt-dc%`-Erzeugung aufrufen; `begin-
  refresh-sequence`/`end-refresh-sequence` auf `(send dc suspend-flush)`/`(send dc resume-flush)`
  verdrahten. Muster 1:1 aus `wx/win32/canvas.rkt:266-330` (gtk-Analogon `wx/gtk/canvas.rkt:644-647`,
  `:672`) — als Referenz LESEN, nicht ändern.
- Kontrakt-Sorgfalt: die HACKING-§4-Regel bleibt gültig (`queue-backing-flush` gibt `(void)` zurück).
  Prüfen, dass die retained-Session `resume-flush`s `(->m void?)`-Kontrakt nicht bricht.

PHASE 3 — NACHHER-Messung (Beweis)
- Exakt denselben Repro. Erwartung: alle getippten Zeilen bleiben sichtbar; Caret-Blink lässt frühere
  Zeilen nicht mehr verschwinden. Vorher/Nachher an den Diskriminatoren gegenüberstellen (retained-
  counter jetzt > 0 während der Sequenz; on-backing-flush verwirft die Bitmap nicht mehr bei jedem
  release-cr).
- Weitere Sicht: mehrzeilig tippen, scrollen, Fenster resizen, Region verdecken/aufdecken — Inhalt
  bleibt durchgehend. Smoke 3/3.
CHECKPOINT WINDOWS (STOPP): Vorher/Nachher-Screenshots + Diskriminator-Diff, Nutzer bestätigt visuell.
Dann Fix committen (zwei Repos, gepusht) und an Linux übergeben.

── LINUX (Validierung, nach Windows-Push) ───────────────

PHASE 4 — Sync + validieren
- ff-Pull qt-backend + main; `git submodule update --init`. ⚠️ SHIM NEU BAUEN
  (`cmake --build .../linux-x64`) — der Fix ist zwar Racket-seitig, aber der Pull bringt auch die
  aktuelle shim.cpp; Bytecode neu (`raco make` mred/draw/qt-queue). Re-Smoke 3/3.
- Theme: Light Mode aktiv bestätigen.
- Denselben Repro (synthetischer Klick via libXtst wie in der Vorsession, oder manuell): bleiben alle
  Zeilen sichtbar? Screenshot gegen helle Baseline. Erwartung: geheilt (gemeinsamer Code).
CHECKPOINT LINUX (STOPP): Ergebnis + Screenshot. Keine Fix-Commits (reine Validierung).

── macOS (Validierung + macOS-only-Beobachtungen, nach Linux) ───

PHASE 5 — Sync + Redraw validieren
- ff-Pull + `git submodule update --init`; ⚠️ SHIM NEU BAUEN (`cmake --build .../macos-arm64`);
  Bytecode neu (stale `compiled/` entfernen, `raco make`). Re-Smoke 3/3.
- Theme: Light Mode aktiv bestätigen (auf macOS besonders wichtig — vorher war es OS-gesteuert und
  hat gestern zu einer Dark-Mode-Fehldeutung geführt).
- Redraw-Repro: bleiben alle Zeilen sichtbar? Screenshot gegen HELLE Baseline. Das ist das
  eigentliche Erfolgskriterium für macOS.

PHASE 6 — macOS-ONLY-Beobachtungen (nur beobachten, nicht fixen)
- Befund B / Startbild: Der frühere „schwarze Balken"-Befund war eine Dark-Mode-Fehldeutung
  (Nutzer-verifiziert: helle Schrift auf dunklem Grund = normaler Dark-Mode). Jetzt mit Light Mode
  erneut ansehen: ist das Startbild (vor jeder Eingabe) sauber? SEPARAT davon der Teil, den das Theme
  NICHT erklärt: die „überlappende/verschachtelte untere Statuszeile" — ist die im sauberen hellen
  Bild noch überlappend, oder war auch das eine Fehldeutung? Nur festhalten.
- Befund A / fehlendes 9. Menü: per System Events erneut auslesen
  (`name of menu bar items of menu bar 1`). Erscheinen jetzt 9 Menüs inkl. „Windows"?
  * Fehlt „Windows" weiterhin: einmal einordnen — auf macOS befüllt Cocoa das „Windows"-Menü teils
    selbst (Fensterliste); ein Fehlen bei nur einem offenen Fenster kann normal sein. Wenn es nach
    diesem Kriterium PLAUSIBEL normal ist → als geklärt notieren. Wenn es nach echtem Aufbaufehler
    aussieht → STOPP und berichten, NICHT fixen.
- Beide Beobachtungen sind Nebenbefunde; sie blockieren das Schließen des Redraw-Themas nicht.
CHECKPOINT macOS (STOPP): Redraw-Ergebnis (Screenshot, helle Baseline) + die zwei Beobachtungen
(Startbild/Statuszeile; Menüzahl mit Einordnung).

OUT OF SCOPE (spätere eigene Sessions)
- Fix für Befund A (fehlendes Menü) und für die Statuszeile, falls sie sich als echt erweist.
- Checkpoint E: Stub-Widgets (tab-panel%, check-box%, choice%, list-box%, slider%, radio-box%) →
  echte Qt-Widgets, getrieben von Dialogen, die DrRacket real öffnet (Preferences, Öffnen/Speichern).

LIEFERUNG & COMMITS
- Windows: der Redraw-Fix in `wx/qt/canvas.rkt` (eigener Commit), zwei Repos, gepusht. Die gated
  Diagnose-Hooks bleiben (nützlich für Regressionsprüfung; nirgends ungegated).
- HACKING.md: neue Lektion — „Redraw/Backing: `wx/qt/canvas.rkt` muss `start-backing-retained` nach
  DC-Erzeugung rufen und `begin-/end-refresh-sequence` auf `suspend/resume-flush` verdrahten (wie
  win32/gtk/cocoa); sonst verwirft jeder Flush die Bitmap → Teil-Invalidierung hinterlässt weiße
  Lücken. Diskriminator: retained-counter (0 = Bug), nicht Blit-Rect/QImage-Größe (beide waren korrekt)."
  §16 (Root-Cause-Kandidat) entsprechend von „Kandidat" auf „bestätigt + gefixt" aktualisieren.
- CLAUDE.md Checkpoint-Tabelle: Redraw als geschlossen markieren, sobald alle drei grün; E-0 damit
  vollständig (Menüs + Redraw). macOS-Nebenbefunde als offene Punkte vermerken.
- STATUS.md je Maschine. macOS/Linux: STATUS-Zeile + `docs/obs-<os>-…md`; keine Fix-Commits.
- Report-Header = gemessene racket --version.