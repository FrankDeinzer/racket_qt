# Report: 2026-07-11-2_prompt (Windows)

**Racket-Version (gemessen):** Welcome to Racket v9.2 [cs].

## 1. Zusammenfassung

Konsolidierungs-Block, Windows-Teil (Phase 0 + Phase 1). Kernbefund: **kein Push nötig** —
beide macOS-Fixe (`acc73108`, `caef3e9c`) waren zu Sessionsbeginn bereits auf
`origin/qt-backend`, und der Umbrella-Zeiger (`759d025` → `origin/main` `12fcfc5`) war
bereits gepusht. Windows-Lokalzustand war lediglich hinter dem eigenen `origin/main`-Stand
zurück (Submodul-Checkout auf altem `19954ffd`). Nach Sync: Bytecode neu, Smoke 3/3 grün,
echtes `PLT_QT=1` DrRacket File → Open + Save As vom Nutzer bestätigt — keine offene Lücke
mehr auf Windows. Linux (Crash-A/B-Rückprüfung, Kernstück des Blocks) und macOS
(nativ-Matrix) bleiben den jeweiligen Maschinen-Sessions vorbehalten, wie im Prompt
vorgesehen.

## 2. Phase 0 — Push-Check

- Git-Status beider Repos zu Sessionsbeginn: Umbrella `main` sauber, `HEAD` = `12fcfc5`,
  bereits `up to date with origin/main`. Gui-Submodul-Arbeitsverzeichnis sauber, aber
  `git status` meldete „behind origin/qt-backend by 2 commits" (lokaler Checkout auf
  `19954ffd`).
- `git fetch origin qt-backend` bestätigt: `caef3e9c` (retained-callbacks) und `acc73108`
  (id-to-menu-item) sind beide bereits auf `origin/qt-backend` — die macOS-Session hatte
  sie also doch gepusht, entgegen der im Prompt vermerkten Unsicherheit „noch nicht
  gepusht".
- Umbrella-Zeiger: `git ls-tree HEAD third_party/gui` → `caef3e9c...`, und dieser
  Zeiger-Commit (`759d025`) ist selbst bereits Teil von `origin/main` (`git fetch
  origin main` zeigt `759d025` zwei Commits unter `origin/main`-HEAD `12fcfc5`).
- **Ergebnis:** Phase-0-Ziel („beide Fixe auf origin + Umbrella-Zeiger") war bereits
  vollständig erfüllt, ohne dass diese Session etwas pushen musste. Einzige Diskrepanz:
  der lokale Submodul-**Checkout** auf dieser Windows-Maschine zeigte noch auf den alten
  Commit — reiner Lokalzustand, kein Sync-Problem zwischen Maschinen.

**CHECKPOINT 0:** ✅ beide Fixe auf origin bestätigt, Umbrella-Zeiger bestätigt. Kein
Push-Schritt nötig.

## 3. Phase 1 — Windows Sync + DrRacket-Lücke

- Nach Nutzer-Bestätigung (`AskUserQuestion`): `git submodule update --init` im Umbrella
  → gui-Submodul lokal von `19954ffd` auf `caef3e9c` gebracht (reiner Checkout eines
  bereits gepushten Commits, kein neuer Commit).
- `git diff --stat 19954ffd..caef3e9c` geprüft: beide Fixe ändern ausschließlich
  `gui-lib/mred/private/wx/qt/menu.rkt` + `platform.rkt` — reiner Racket-Code, **kein**
  Shim/C++ betroffen. Shim-Rebuild daher nicht nötig.
- `raco make` gezielt auf die zwei geänderten Dateien: erfolgreich, kompiliert auch
  transitive Abhängigkeiten (`frame.rkt`, `canvas.rkt`, `menu-bar.rkt`, `menu-item.rkt`,
  `queue.rkt` u. a.).
- `raco setup mred framework`: Compile-Phase erneut erfolgreich, **aber** die
  Launcher-Neuerzeugung (`mred-text.exe`) schlägt mit `Zugriff verweigert` fehl — dieser
  PowerShell-Prozess läuft nicht elevated, Schreibzugriff auf `C:\Program Files\Racket\`
  braucht Admin-Rechte (siehe `docs/HACKING.md` §13, bekanntes Muster, kein neuer Bug).
  Da die eigentliche Bytecode-Kompilation vor diesem Schritt bereits durchlief, ist das
  für den Fix folgenlos — nur die (hier nicht benötigte) Launcher-Regeneration betroffen.
- Re-Smoke: `racket -l raco -- test tests/smoke.rkt` mit `PLT_QT=1` → **3/3 grün**
  (einzige Nebenausgabe: Qt-DPI-Awareness-Warnung, bekannt/harmlos).
- **DrRacket-Lücke nachgeholt:** `PLT_QT=1 DrRacket.exe` gestartet (PID 4280), Start-
  Latenz abgewartet (~8 s, Prozess weiter lebendig geprüft vor Interaktion — Lehre aus
  der Linux-Session §6.1 beachtet), Nutzer hat File → Open **und** File → Save As real
  durchgeführt: „laden und speichern hat funktioniert". Prozess nach beiden Aktionen
  weiterhin am Leben (kein Crash).
- Windows Qt-eigen×nativ-Matrix bereits aus Vorsession komplett (7/7 nativ,
  `docs/2026-07-11_report-win.md` §5) — hier nur vermerkt, nicht wiederholt.

**CHECKPOINT 1:** ✅ Windows DrRacket File → Open/Save grün, Sync bestätigt.

## 4. Offene Punkte / Out of Scope (dieser Session)

- **Linux Crash-A/B-Rückprüfung (Phase 2)** — das eigentliche Kernstück des
  Konsolidierungs-Blocks laut Prompt — NICHT auf dieser Maschine ausgeführt, folgt in
  eigener Linux-Session.
- **Linux/macOS Qt-eigen×nativ-Matrix (Phase 3/4)** — folgt in den jeweiligen Sessions.
- `docs/HACKING.md` §19 Crash-A/B-Status **bewusst nicht** aktualisiert — die
  Verifikation ist Linux-seitig noch offen, ein Update wäre eine gesetzte Annahme statt
  eines Messschritts (Prompt-Vorgabe).
- Vorbestehende Nebenbefunde unverändert offen (macOS-Menüleiste, Linux-Resize/KWin,
  Windows-Toolbar-Icon-Timing, htdp-lib-Crash) — nicht Teil dieses Blocks.
