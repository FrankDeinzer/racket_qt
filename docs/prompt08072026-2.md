ROLLE & ZIEL
Spur 1 nach dem Menü-Titel-Fix (report08072026): (1) den Gewinn sichern — Windows-Push + Cross-
Platform-Validierung des Titel-Fixes + DrRacket-Gegencheck — und (2) den NEUEN Klick-Bug (Klick auf
Menütitel öffnet kein Dropdown) als MESSUNG charakterisieren. Der Klick-Bug wird hier NICHT gefixt
(das ist Spur 2, nach dieser Messrunde und mit externem Review).

STAND (aus report08072026 / STATUS)
- Titel-Fix (leerer QMenu-Titel → Balkenhöhe 0) behoben via `shim_menu_set_title`-Durchreichung.
  Committet: gui `6083efc9` (menu-bar.rkt, utils.rkt), Umbrella `2c102e5` (shim.cpp + gated Diagnose).
  ⚠️ NUR LOKAL — noch nicht gepusht. macOS/Linux haben den Fix nicht.
- Menüleiste auf Windows sichtbar + horizontal (File@0/Edit@52/Help@107), nutzerbestätigt.
- NEUES Problem: Klick auf Top-Level-Titel öffnet kein Dropdown; Hover hebt hervor (Balken lebt).
  `activePopupWidget()` war nie ≠ null → das Popup wird nie GEZEIGT (nicht: gezeigt+geschlossen; nicht:
  fehlplatziert). Charakterisiert als Event-Loop-Familie, evtl. gemeinsame Wurzel mit dem Redraw-Bug.
- Alle drei Maschinen auf gui-lib 1.80 in Parität (Stand 07-07). macOS/Linux konsumieren via
  `-S`-Source-Override (raco make), Windows via Installation-wide-Link (DrRacket verlinkt).

HARTE GUARDRAILS
- Klick-Bug: MESSEN, NICHT FIXEN. Auch wenn die Messung die Fix-Richtung zeigt — kein Fix in dieser
  Session (Fix kann das Event-Loop-Modell berühren → Spur 2, braucht Cross-Platform-Daten + Review).
- Kein `exec()`, kein `QEventLoop`, keine geschachtelte/„bis idle"-Schleife. Die Mess-Hooks
  (direkter `QMenu::popup()`, `activeWindow()`-Print, Tastatur-Trigger) sind EINMAL-Aufrufe/Ausgaben
  hinter `PLT_QT_DEBUG`, KEINE Loop-Änderung — das ist erlaubt und guardrail-konform.
- cocoa/gtk/win32 NICHT anfassen. Redraw-Bug NICHT anfassen. `mapToGlobal`/client-to-screen-
  Positionierung NICHT hier (gehört mit dem Klick-Fix in Spur 2). Zwei-Repo-Commits. Nur ff-Pulls,
  kein rebase/force/reset.

KOLLABORATION (visuell): Klick-Öffnen ist nicht zuverlässig automatisch prüfbar — der NUTZER klickt
und berichtet, ob ein Dropdown erscheint; die instrumentierten Diskriminatoren erklären das WARUM.
Nicht raten, ob es „richtig aussieht" — zeigen und messen.

PHASE 0 (alle Maschinen) — erkennen & verifizieren
- OS/Arch; `racket --version` MUSS v9.2 [cs] sein — GEMESSEN, nicht aus einem kopierten Header.
- Git-Status beider Repos (Branch, sauber, Zeiger==HEAD).
- HYGIENE (dauerhaft gegen Versionsdrift): Report-Header dieser Session = die in Phase 0 GEMESSENE
  `racket --version`, nicht ein übernommener Wert.

── WINDOWS (führt: Push, DrRacket-Gegencheck, Klick-Messung, Doku) ────────────

W1 — PUSH (Gewinn sichern, Voraussetzung für macOS/Linux)
- Ist `6083efc9`/`2c102e5` bereits auf origin? Falls nein: pushen (gui-Submodul zuerst, dann Umbrella).
  Falls ja: vermerken, weiter. (Gate-Test bleibt gültig: DrRacket ohne PLT_QT startet nativ.)

W2 — Phase 3: DrRacket-Gegencheck (verbindet Minimal-Fix mit echtem Ziel)
- `PLT_QT=1 DrRacket.exe` starten (vorher `tasklist | findstr /i drracket` wg. Single-Instance-Falle;
  graceful via WM_CLOSE beenden, kein Hard-Kill → keine Autosave-Recovery, HACKING §13).
- Rendert die Menüleiste jetzt horizontal (File/Edit/View… über die Breite) statt gestapelt auf
  gleicher x-Origin? Screenshot + Nutzer-Urteil. Rest-Item-Layout-Probleme FLAGGEN, nicht ausfeilen.

W3 — Klick-Bug MESSEN (kein Fix; Hooks hinter PLT_QT_DEBUG)
Prior (report02072026): knopf-getriggerte Popups erschienen unter demselben pump-Modell (DrRacket-
Sprach-Dropdown öffnete — mispositioniert; Kontextmenü erschien lokal). Erwartung daher: das Modell
KANN Popups zeigen; der Fehler ist spezifisch QMenuBars Klick→Submenü. Bestätige/widerlege mit vier
Diskriminatoren an `examples/menu-frame.rkt` (bzw. isoliertem File/Edit/Help-Menü ohne editor-canvas%):
  1. DIREKT `QMenu::popup(pos)` auf das File-Menü aufrufen (Klick komplett umgehen) — minimaler gated
     Shim-Hook oder über den vorhandenen menu%-popup-Pfad. Erscheint ein Dropdown?
       • JA (erwartet) → pump zeigt Popups; Bug isoliert auf QMenuBar-Klick-Aktivierung.
       • NEIN → tiefer: Modell bringt Popup-Fenster unter pump(0)/50 ms nicht zum Leben (dann Wurzel
         geteilt mit Redraw-Bug wahrscheinlich).
  2. `QApplication::activeWindow()` drucken, wenn Fenster gezeigt + fokussiert: null oder der Frame?
     (QMenuBar-Popups brauchen oft ein aktives Fenster; Windows blockierte früher `SetForegroundWindow`
     → starke, mundane Spur, falls null.)
  3. Tastatur-Aktivierung (F10 bzw. Alt+F via SendKeys): öffnet die Leiste per Tastatur, obwohl der
     Klick es nicht tut? (Maus-vs-Tastatur-Diskriminator: Tastatur öffnet + Maus nicht → Press-Routing/
     Grab; keins von beiden → Aktivierungs-/Fokus-Zustand.)
  4. Nutzer klickt den Titel (Kern-Beobachtung): öffnet ein Dropdown? Wenn nein, konsistent mit dem
     Log aus (1)–(3).
Alle vier Ergebnisse berichten. KEIN Fix.

W4 — Doku nachziehen
- HACKING.md: die KORRIGIERTE Menü-Lektion eintragen (und sicherstellen, dass die FALSCHE — „QMenuBar
  bekommt unter pump nie eine Layout-Runde; Fix = Layout-Trigger; Diskriminator height()" — NICHT
  drinsteht):
    „Menüleiste — leerer Menütitel kollabiert den Balken. `menu-bar% append` bekam den Titel, reichte
    ihn aber nie an den QMenu. `QMenuBar::addMenu(QMenu*)` leitet den Item-Text aus dem Menütitel ab →
    leerer Titel = 0×0-Action-Rect = Balkenhöhe 0 auf allen drei Plattformen (gemeinsamer Racket-Pfad,
    oberhalb der Plattform). Fix = Titel via `shim_menu_set_title` durchreichen; KEIN Layout-Trigger,
    KEIN setNativeMenuBar, KEINE Geometrie-Reservierung. Diskriminator ist die Action-TEXT-Länge
    (0×0-Rect), NICHT `QMenuBar::height()`."
- CLAUDE.md Checkpoint-Tabelle: E-0-Menü differenzieren — „Menüleiste sichtbar + horizontal (Titel-Fix,
  Windows bestätigt); OFFEN: Klick öffnet kein Dropdown; Cross-Platform-Validierung (Phase 4) läuft".
  Flags aktualisieren (Titel-Fix erledigt; Klick-Öffnen neu; mapToGlobal/Redraw weiterhin offen).
- STATUS.md: Eintrag für die 07-08-Session (Titel-Fix + Klick-Bug) UND für diese Session.
CHECKPOINT WINDOWS (STOPP): Push bestätigt; DrRacket-Urteil (Screenshot); die vier Klick-Messungen;
Doku aktualisiert.

── macOS / LINUX (erst NACH Windows-Push) ─────────────────────────────────────

M1 — Sync auf den Titel-Fix
- Verifiziere `6083efc9` ist auf `origin/qt-backend`. Fehlt → STOPP („Windows hat noch nicht gepusht").
- ff-Pull qt-backend + main; `git submodule update --init`.
- ⚠️ SHIM NEU BAUEN (`cmake --build .../<preset>`): Der Fix fügte `shim_menu_set_title` NEU hinzu — ohne
  Rebuild scheitert die FFI-Bindung an fehlendem Symbol (genau die stale-Shim-Falle aus 07-07). Danach
  Bytecode neu (`raco make` mred/draw/qt-queue, da -S-Override) und re-smoke 3/3.

M2 — Phase 4: Titel-Fix validieren (mit PLT_QT_DEBUG)
- `menu-frame.rkt` starten (Plattform-Startrezept: Linux QT_PLUGIN_PATH; macOS RPATH im Shim):
  • Linux: `mb.height()` jetzt > 0, Balken im Fenster sichtbar, Titel horizontal? Nutzer bestätigt.
  • macOS: erscheinen jetzt File/Edit in der GLOBALEN Leiste? Per System Events auslesen
    (`name of menu bar items of menu bar 1`) — Erwartung: nicht mehr nur `{Apple, racket}`.
- Erwartung: sichtbar/vorhanden, weil der Titel nicht mehr leer ist. Falls Linux MIT korrektem Titel
  WEITER Höhe 0 zeigt → messgestützt melden (dann wäre dort DOCH ein Layout-Trigger nötig — aber erst
  dann, nicht spekulativ).

M3 — Klick-Bug messen (dieselben Diskriminatoren, cross-platform)
- Nutzer klickt Titel: öffnet ein Dropdown? Direkter `QMenu::popup()`-Test: erscheint es? `activeWindow()`
  drucken: null? (macOS: ein nicht-gebündelter CLI-Prozess bekommt oft keinen Fokus → wenn activeWindow
  null ist, starke Spur, dass Aktivierung die Ursache ist, nicht das Pump-Modell.)
- Ergebnis ist der Cross-Platform-Datenpunkt: Klick überall kaputt → Wurzel im gemeinsamen Modell/
  QMenuBar; nur Windows → plattformspezifisch (Aktivierung).
CHECKPOINT je Plattform (STOPP): Sync-Status; Phase-4-Zahlen + Screenshot + Nutzer-Urteil; die Klick-
Messungen. Keine Doku-Churn außer einer STATUS-Zeile + `docs/obs-<os>-08072026.md` (distinkte Namen →
konfliktfrei); KEINE neuen Fix-Commits auf diesen Maschinen.

OUT OF SCOPE (bewusst, spätere Sessions)
- Klick-Bug-FIX — Spur 2, nach dieser Messrunde. Falls die Messung aufs Event-Loop-Modell zeigt: Fix
  über den WASM-verträglichen Wakeup-Mechanismus (statt 50-ms-Poll + events_pending→0), NIEMALS exec().
- `mapToGlobal`/client-to-screen-Positionierung — gehört mit dem Klick-Fix in Spur 2.
- Redraw-Bug (nur zuletzt getippte Zeile rendert, kein persistenter Backing-Store) — eigene Session.
- macOS globale-Menü-Politur / DrRacket-Item-Layout jenseits der Phase-3-Beobachtung.

LIEFERUNG & COMMITS
- Windows: Push des Titel-Fixes; Doku (HACKING korrigiert, CLAUDE-Tabelle, STATUS 07-08 + heute);
  gated Mess-Hooks (hinter PLT_QT_DEBUG, nirgends ungegated). Zwei-Repo-Disziplin.
- macOS/Linux: STATUS-Zeile + `docs/obs-<os>-08072026.md`; keine Fix-Commits.
- Report-Header = gemessene `racket --version`.

