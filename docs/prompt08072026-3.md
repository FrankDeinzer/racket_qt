ROLLE & ZIEL
Spur 2 — Menüs voll funktional machen, in EINER Session mit interner Stufung:
  (1) addAction-Fix: Blatt-Menüeinträge erscheinen (Root-Cause aus report08072026-2 verifiziert).
  → CHECKPOINT A (self-gate).
  (2) NUR falls A sauber: mapToGlobal/client-to-screen — Popups/Dropdowns an korrekter Bildschirm-
      position.
FIX-Modus (mit Messung IM Fix), nicht nur messen. Beide Fixes sind reine Qt-Objektstruktur bzw.
Koordinaten-Mapping — sie berühren das Event-Loop-Modell voraussichtlich NICHT.

QT-SKILLS NUTZEN
Für die QMenu/QAction- und mapToGlobal-Arbeit die installierten Qt-Skills konsultieren, bevor du
Shim-Code schreibst — besonders zu (a) QAction-Ownership/Lifetime bei `QMenu::addAction(QAction*)`
und (b) `QWidget::mapToGlobal` vs. `mapToScreen`/Multi-Screen-Koordinaten. Nicht raten, wo der Skill
die Semantik klärt.

STAND (aus report08072026-2, verifiziert)
- Titel-Fix gepusht, Menüleiste in echtem DrRacket horizontal bestätigt (E-0-Sichtbarkeit erledigt).
- Klick-Bug ist KEIN Event-Loop-Thema: direkter `QMenu::popup()` auf ein gemischtes Menü zeigte
  `popup APPEARED visible=1` → pump zeigt Popups. Ursache: `shim_action_create` erzeugt die QAction,
  ruft aber nie `QMenu::addAction()` (`grep addAction shim.cpp` = 0). Submenüs werden via
  `shim_menu_add_submenu`→`addMenu` hinzugefügt, Blatt-Items nicht → Menüs zeigen nur Submenü-Einträge.
- Alle drei Maschinen auf gui-lib 1.80 in Parität; macOS/Linux via `-S`-Override (raco make),
  Windows via Installation-wide-Link.

HARTE GUARDRAILS
- Kein `exec()`, kein `QEventLoop`, keine geschachtelte/„bis idle"-Schleife. Wenn sich beim addAction-
  oder mapToGlobal-Fix zeigt, dass ein Menü-Verhalten DOCH das Event-Loop-Modell berühren würde → STOPP
  und berichten (nicht das Modell antasten).
- cocoa/gtk/win32 NICHT anfassen. Redraw-Bug NICHT anfassen. Zwei-Repo-Commits (Submodul zuerst).
  Nur ff-Pushes/-Pulls, kein rebase/force/reset. Report-Header = in Phase 0 GEMESSENE `racket --version`.
- Gated Diagnose (`PLT_QT_DEBUG`) bleibt gated; nichts ungegated lassen.

── WINDOWS (führt) ──────────────────────────────────────

PHASE 0 — verifizieren
- `racket --version` MUSS v9.2 [cs] sein (gemessen). Git beide Repos sauber, Zeiger==HEAD, auf origin.

PHASE 1 — addAction-Fix
1a. ERST reconciliieren, nicht sofort neu bauen: Prüfe, was im Menü-Pfad bereits existiert.
    - `menu%.append` in `gui-lib/mred/private/wx/qt/menu.rkt` (Blatt- vs. Submenü-Zweig).
    - `shim_action_create` in `shim.cpp`.
    - Existiert aus der E-0-Menü-Implementierung noch `shim_menu_append_action` (report300626 listete
      es unter den 15 Funktionen)? Wenn ja: ist es tot/nie aufgerufen, oder ein abgehängter Pfad?
    ZIEL: EINEN korrekten Weg herstellen (die vorhandene Funktion reparieren/verdrahten, wenn sie den
    Zweck erfüllt), NICHT einen dritten parallelen Weg bauen.
1b. Fix, gespiegelt am funktionierenden Submenü-Pfad (`shim_menu_add_submenu`→`addMenu`): die erzeugte
    Blatt-`QAction` muss per `QMenu::addAction()`/`insertAction()` zum `qt-menu`.
    OWNERSHIP (Qt-Skill konsultieren): `new QAction(label)` ist aktuell parentlos; `addAction(QAction*)`
    übernimmt KEIN Ownership. Action der Menü parenten (z. B. `new QAction(label, menu)` bzw. setParent)
    ODER den ownership-übernehmenden Pfad nutzen — so, dass keine Leaks/Doppel-Frees entstehen und der
    Lifetime zu Rackets `item-table`-Handle konsistent bleibt. Am Submenü-Pfad spiegeln.
1c. MESSUNG IM FIX (an `examples/menu-click-probe.rkt` gated + echtem DrRacket):
    - Blatt-only-Menü (File→Quit): direkter `popup()` zeigt jetzt das Item; `actions().size()`==1,
      Höhe entspricht einem Eintrag.
    - Gemischt (New + Recent-Submenü + Save): alle drei sichtbar, korrekte Reihenfolge (order-push!).
    - Separatoren (`shim_menu_append_separator`) an richtiger Stelle.
    - Checkables: `set-checked`/`is-checked` spiegeln sich visuell.
    - DYNAMIK (DrRacket schaltet Menüpunkte laufend): enable/disable (ausgegraut), check, delete-action
      über die vorhandenen item-table-Dispatch-Funktionen — funktionieren sie jetzt, wo die Items
      wirklich im Menü hängen?
    - Echtes DrRacket: zeigen File/Edit/… jetzt ihre Blatt-Items (Open, Save, Undo …)?
1d. addAction SEPARAT committen (eigener Commit = sauberer Rollback-Punkt), zwei Repos, gepusht.

CHECKPOINT A — self-gate (programmatisch, keine Wartepause nötig)
GO nur wenn ALLES zutrifft:
  - `popup()`-Probe: Blatt-only-Menü hat jetzt actions().size()==erwartete Zahl; gemischtes Menü zeigt
    Blatt- UND Submenü-Einträge in richtiger Reihenfolge.
  - enable/check/delete-Dispatch wirkt sichtbar; Separatoren/Checkables korrekt.
  - Smoke 3/3 grün; kein Crash, kein Ownership-Verdacht (kein Doppel-Free/Warning beim Schließen).
  - DrRacket-Menüs sind gefüllt (per Screenshot festgehalten).
→ Alle grün UND keine Anomalie: AUTONOM weiter zu Phase 2. Der Nutzer bestätigt die Visuals am Ende
  für BEIDE Fixes gemeinsam.
→ Ein Kriterium fehlgeschlagen ODER etwas Unerwartetes (v. a. wenn ein Menü-Verhalten das Event-Loop-
  Modell zu berühren scheint, oder Ownership-Crash): STOPP, berichten, warten.

PHASE 2 — mapToGlobal / client-to-screen (nur bei GO)
2a. `client-to-screen` ist aktuell No-op (CLAUDE.md-Flag). Neue Shim-Funktion
    `shim_widget_client_to_screen(void* handle, int x, int y, int* out_x, int* out_y)` (oder gepackte
    Rückgabe) → `QWidget::mapToGlobal(QPoint(x,y))`. Qt-Skill: mapToGlobal vs. mapToScreen, Multi-
    Screen; DPR ist auf 1 gepinnt → device-independent px konsistent, kein Skalierungs-Sonderfall hier.
2b. In `window.rkt` `client-to-screen` echt verdrahten (die Stelle, die `popup-menu` und das Control-
    Dropdown nutzen). FFI-Binding in `utils.rkt`.
2c. MESSUNG: Kontextmenü (Rechtsklick im Editor) erscheint jetzt am Cursor statt am Bildschirmrand?
    Control-Dropdown (z. B. Sprachauswahl in DrRacket) öffnet unter dem Control? Nutzer bestätigt visuell.
2d. mapToGlobal SEPARAT committen, zwei Repos, gepusht.

CHECKPOINT B (STOPP): Nutzer bestätigt beide Visuals — gefüllte Menüs (Phase 1) + korrekt platzierte
Popups (Phase 2). Screenshots. Dann Doku (unten) und Übergabe an macOS/Linux.

── macOS / LINUX (nach Windows-Push) ────────────────────

PHASE 3/4 — Cross-Platform-Validierung BEIDER Fixes
- ff-Pull qt-backend + main; `git submodule update --init`.
- ⚠️ SHIM NEU BAUEN (`cmake --build .../<preset>`) — beide Fixes änderten shim.cpp; ohne Rebuild
  scheitert die FFI an fehlenden Symbolen (stale-Shim-Falle 07-07). Danach `raco make` (mred/draw/
  qt-queue), re-smoke 3/3.
- Validieren mit `PLT_QT_DEBUG`:
  * Linux (in-window): Menüs zeigen jetzt Blatt-Items im Dropdown; Kontextmenü/Control-Dropdown an
    korrekter Position (mapToGlobal).
  * macOS (globale Leiste): zeigen die Menüs in der Systemleiste jetzt ihre Blatt-Items? Kontextmenü/
    Control-Popup an korrekter Position? (Für die Top-Level-Systemleiste ist mapToGlobal weniger
    relevant, für Kontextmenüs/Control-Popups schon.)
- Beide Fixes sind gemeinsamer Racket/Shim-Pfad → Erwartung: wirken cross-platform wie der Titel-Fix.
  Abweichung messgestützt melden, nicht spekulativ nachbessern.
CHECKPOINT je Plattform (STOPP): Sync + Rebuild bestätigt, Smoke 3/3, die zwei Validierungen + Nutzer-
Urteil. Keine Fix-Commits auf diesen Maschinen; STATUS-Zeile + `docs/obs-<os>-…md` (distinkte Namen).

OUT OF SCOPE (spätere eigene Sessions)
- Redraw-Bug (nur zuletzt getippte Zeile rendert, kein persistenter Backing-Store) — der nächste große
  Blocker nach den Menüs; eigene Session.
- Nebenbefund `activeWindow=NULL` + kurzes `popup APPEARED`→`GONE`: nach dem addAction-Fix an ECHTEM
  DrRacket erneut messen (nicht am CLI-Probe-Skript, das keinen Fokus hält). Wahrscheinlich Fokus-
  Artefakt des Probe-Skripts, nicht der Menüs — erst nach dem Fix beurteilen.

LIEFERUNG & COMMITS
- Zwei getrennte Commits (addAction; mapToGlobal), beide Repos, gepusht.
- HACKING.md: neue Lektion — „Menü-Blatt-Items: `shim_action_create` erzeugte die QAction ohne
  `QMenu::addAction` → Items für Qt unsichtbar (Dropdown zeigte nur Submenüs). Fix = addAction mit
  korrektem QAction-Ownership (Menü als Parent), gespiegelt am `addMenu`-Submenü-Pfad. Plus
  `shim_widget_client_to_screen`→`mapToGlobal` behebt Popup-/Dropdown-Position (client-to-screen war
  No-op)." Beides sind gemeinsame Racket/Shim-Pfade, cross-platform.
- CLAUDE.md Checkpoint-Tabelle: E-0-Menü als geschlossen markieren, sobald Phase 3/4 grün (sichtbar +
  Items + Position, alle drei Plattformen). Flags: Titel/addAction/mapToGlobal erledigt; Redraw offen.
- STATUS.md je Schritt. Report-Header = gemessene racket --version.

