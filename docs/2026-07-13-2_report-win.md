# Report — Block A: `choice%`/`radio-box%`/`slider%` echt (orakelgetrieben) — 2026-07-13-2 (Windows)

**Racket:** `v9.2 [cs]` (gemessen, `racket --version`).

## Phase 0 — Umgebung

- Sync bereits korrekt: gui-Submodul (`third_party/gui`) @ `caef3e9c`, Umbrella-Zeiger
  (`third_party/gui`-Einträg im Umbrella-HEAD) passend — kein Pull nötig.
- Shim-Aktualität: `qt-shim/build/windows-x64/Debug/racketqtshim.dll` (mtime
  1783768741) neuer als `qt-shim/src/shim.cpp` (mtime 1783768715, vor dieser Sitzung) —
  kein Rebuild vor dieser Sitzung nötig; wurde im Rahmen der Sitzung ohnehin neu gebaut
  (neue Shim-Funktionen).
- Light Mode bestätigt: `framework:white-on-black?` = `#f` in
  `%APPDATA%\Racket\racket-prefs.rktd` (Legacy-Key `framework:color-scheme` = `classic`,
  nicht verwendet).
- Re-Smoke vor Änderungen: 3/3 grün.

## Phase 1 — Orakel

Quelle: `third_party/gui/gui-lib/framework/private/preferences.rkt` +
`third_party/gui/gui-lib/framework/private/color-prefs.rkt` (echte DrRacket-
Preferences-Panels, kein Umweg nötig — anders als der Autosave-Fehlgriff in §18.1).

**Widget-Inventar (real gezogen):**

| Widget | Fundstelle | Kontext |
|---|---|---|
| `slider%` | `preferences.rkt:439` (`make-recent-items-slider`) | Anzahl zuletzt geöffneter Dateien, 1–100 |
| `slider%` | `preferences.rkt:834` | Editor-Schriftgröße, 1–127 |
| `radio-box%` | `preferences.rkt:606` (`add-boolean-option-with-ask-me`) | generische Boolean-„ask me"-Optionen |
| `radio-box%` | `preferences.rkt:661` (`print-rb`) | Druckmodus |
| `radio-box%` | `color-prefs.rkt:1343` (`mk-color-scheme-radio-buttons`) | Farbschema-Auswahl, **1-Button-Gruppen, `[selection #f]`** |
| `choice%` | `color-prefs.rkt:149` (`smoothing-menu`) | Text-Schriftglättung |
| `choice%` | `color-prefs.rkt:498` | Klammer-Farbschema |
| `choice%` | `color-prefs.rkt:1282`/`1295` | Farbmodus (Windows: Light/Dark; sonst: OS/Light/Dark) |

**Kernbefund (Eigenabhängigkeit):** `preferences.rkt`s eigene Dialog-Navigation
(`make-tab/single-panel`, Zeile ~321, aufgerufen für JEDE Panel-Ebene inkl. der
obersten) instanziiert unconditional ein `tab-panel%`. `tab-panel%` ist weiterhin Stub
(`make-stub-class`) — Block B, eigener Prompt. Damit ist eine Ende-zu-Ende-Validierung
über den echten Preferences-Dialog **strukturell blockiert**, unabhängig von den drei
Ziel-Widgets selbst. Genau der in Phase 1 des Prompts antizipierte Fall — kein Stopp,
kein Scope-Bruch, Beweis läuft über einen isolierten Probe statt über den vollen Dialog.

**Checkpoint 1: GO (autonom)** zu Phase 2.

## Phase 2 — Implementierung (Windows)

Kontrakt-Methoden gegen `wx/gtk/{choice,radio-box,slider}.rkt` UND
`wx/win32/{choice,radio-box,slider}.rkt` verifiziert (nicht geraten):

| Widget | Init-Args (verifiziert) | Methoden |
|---|---|---|
| `choice%` | `parent cb label x y w h choices style font` | `set-selection`/`get-selection`/`number`/`clear`/`append`/`delete` |
| `radio-box%` | `parent cb label x y w h labels val style font` | `set-selection`/`get-selection`/`number`/`enable-button`/`button-focus` |
| `slider%` | `parent cb label val lo hi x y w style font` | `set-value`/`get-value` |

Details, Signal-Muster, `QSignalBlocker`-Konventionen, der `radio-box%`-Dummy-Button-Fix
für `set-selection #f` (QButtonGroup-Doku via MCP-Tool verifiziert statt angenommen) und
die `handles-key-code`-Kontrollprobe (public\*/override\*-Invariante):
`docs/HACKING.md` §20.

**Shim (`qt-shim/src/shim.cpp`):** neue Funktionen `shim_slider_{create,set_value,
get_value}`, `shim_choice_{create,append,clear,delete,count,set_selection,
get_selection}`, `shim_radio_box_{create,append_button,set_selection,get_selection,
enable_button,button_focus}`. Neue Includes: `QComboBox`, `QRadioButton`,
`QButtonGroup`, `QBoxLayout`, `QVariant`, `QSlider`. DLL neu gebaut, kompiliert clean.

**Racket-Seite:** drei neue Dateien (`wx/qt/{choice,radio-box,slider}.rkt`), FFI-Bindings
in `wx/qt/utils.rkt`, Wiring in `wx/qt/platform.rkt` (drei `make-stub-class`-Einträge
durch echte Imports ersetzt). `raco setup mred` kompilierte die Collection-Bytecode
erfolgreich (Zwischenschritt „creating launchers" scheiterte an einem
Zugriffsverweigert-Fehler beim Überschreiben von `mred-text.exe` — nicht-blockierend,
betrifft nur einen Standalone-Launcher, nicht das für `racket`/`raco test` relevante
Bytecode; bestätigt über `.zo`-Zeitstempel im verlinkten Paketpfad).

## Phase 3 — Preferences als Beweis

Wie in Phase 1 festgestellt: der echte Dialog ist durch `tab-panel%` blockiert. Dieser
Schritt ist damit **auf „nach Block B" vermerkt**, wie im Prompt vorgesehen. Beweis
stattdessen über `examples/value-widgets-probe.rkt` (neu, analog
`dialog-widgets-probe.rkt`): alle drei Widgets + je ein „set via code"-Button pro
Widget, inkl. eines 1-Button-`radio-box%` mit `[selection #f]` (härtester Fall).

**Nutzer-Bestätigung (visuell/interaktiv):** „All looks good. all components behave
correctly." — Rendering, Interaktion (Dropdown-Auswahl, Radio-Klick, Slider-Drag) und
die „set-\* löst den eigenen Callback nicht erneut aus"-Anforderung wurden explizit
geprüft. Konsolen-Log der Probe-`printf`s selbst nicht eingefangen — PowerShell/
`Out-String` puffert die komplette Pipe eines noch laufenden Hintergrundprozesses,
unabhängig von Rackets `file-stream-buffer-mode` (reine Tooling-Einschränkung dieser
Sitzung, kein Produkt-Befund). Die visuelle/interaktive Bestätigung deckt dieselben
Kriterien ab und ist der in diesem Projekt etablierte Verifikationsstandard.

## Verifikation

- Klassen-Komposition lädt fehlerfrei (kein `method already defined`/`no method to
  override`) — bestätigt die `public*`/`override*`-Invariante wurde nicht verletzt.
- Smoke 3/3 grün vor der Sitzung, 3/3 grün nach allen drei Commits + Rekompilierung.
- Probe Nutzer-bestätigt (s. o.).

## Guardrails

Keine Shared-Code-Änderung. Keine `tab-panel%`-Mitimplementierung (Stub bleibt stehen,
Befund dokumentiert). Kein `exec()`/keine geschachtelte Event-Loop. Nicht
cross-platform in dieser Sitzung.

## Commits

**gui-Submodul (`qt-backend`), lokal, noch nicht gepusht** (getrennt pro Widget,
Rollback-Punkte, Nutzer-Präferenz):

- `dc5cef02` — `feat(qt): slider% real (QSlider)`
- `3a2b2d8e` — `feat(qt): choice% real (QComboBox)`
- `3ba8fa75` — `feat(qt): radio-box% real (QRadioButton + QButtonGroup)`

Lokale `qt-backend`-Branch-Referenz war vor Sitzungsbeginn stale (`19954ffd`, 2 Commits
zurück) und wurde nach Nutzer-Bestätigung per Fast-Forward auf `caef3e9c` (=
`origin/qt-backend`) korrigiert, bevor die drei Commits draufgesetzt wurden — reine
lokale Zeiger-Bewegung, kein Netzwerkzugriff.

**Umbrella (`main`), noch nicht committed:** `qt-shim/src/shim.cpp` (neue
Shim-Funktionen), `examples/value-widgets-probe.rkt` (neu), `docs/HACKING.md` (§20),
`CLAUDE.md` (Checkpoint-Tabelle), `STATUS.md` (neuer Eintrag), dieser Report.

## Nächster Schritt

`tab-panel%` (Block B, eigener Prompt) — danach erst volle Preferences-Dialog-
Ende-zu-Ende-Validierung möglich. macOS/Linux-Validierung dieser drei Widgets: separater
Prompt nach Push (wie im Auftrags-Prompt vorgesehen). Push/Sync-Entscheidung: siehe
Nutzer-Rückfrage am Ende dieser Sitzung.
